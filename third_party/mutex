// Windows-only stub for MinGW GCC 6.3.0.
// On Linux (Docker/Render), real <mutex> from GCC is used.
#ifdef _WIN32
#pragma once
namespace std {
    class mutex { public: void lock() {} void unlock() {} };
    class recursive_mutex { public: void lock() {} void unlock() {} };
    template<typename T> class lock_guard { public: lock_guard(T&) {} };
    template<typename T> class unique_lock { public: unique_lock(T&) {} };
}
#endif
