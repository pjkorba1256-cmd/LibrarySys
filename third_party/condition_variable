// Windows-only stub for MinGW GCC 6.3.0.
// On Linux (Docker/Render), real <condition_variable> from GCC is used.
#ifdef _WIN32
#pragma once
namespace std {
    class condition_variable {
    public:
        void notify_one() {}
        void notify_all() {}
        template<typename Lock> void wait(Lock&) {}
        template<typename Lock, typename Predicate> void wait(Lock&, Predicate) {}
    };
}
#endif
